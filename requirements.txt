# Core agent stack
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-mcp-adapters>=0.3.0
langchain-groq>=0.2.0
mcp[cli]==1.29.0

# Retrieval
chromadb>=0.5.0
scikit-learn>=1.4.0          # only needed for the offline TF-IDF fallback embedder
requests>=2.31.0             # for downloading real dataset images (build_image_index.py)

# Production embedding backends (swap in for TfidfEmbedder — see agent/embeddings.py)
sentence-transformers>=3.0.0
open_clip_torch>=2.24.0
torch>=2.2.0

# UI
streamlit>=1.38.0
pillow>=10.0.0

# Optional: local LLM fallback if you don't want to use Groq
# langchain-ollama>=0.2.0

# Optional: evaluation
# ragas>=0.2.0
# datasets>=2.20.0